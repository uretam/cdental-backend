server:
  port: 9081

spring:
  application:
    name: pacientes
  datasource:
    url: jdbc:mysql://localhost:3306/cdent_pacientes?serverTimezone=UTC&useSSL=false&createDatabaseIfNotExist=true
    username: root
    password: 
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.xml